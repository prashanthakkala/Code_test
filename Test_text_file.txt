<!DOCTYPE html>
<html>
<head>
    <title>FHPS Application</title>

    <style>
        body {
            margin: 0;
            height: 100vh;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            background-color: #f4f6f9;
        }

        /* Header */
        .header {
            background-color: #007bff;
            color: white;
            padding: 15px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-size: 22px;
            font-weight: bold;
        }

        /* Home button */
        .home-btn {
            background-color: white;
            color: #007bff;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            font-size: 14px;
            cursor: default; /* showcase only */
        }

        /* Center section */
        .center-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 25px;
        }

        /* Info cards */
        .cards {
            display: flex;
            gap: 20px;
        }

        .card {
            background-color: white;
            padding: 15px 20px;
            border-radius: 8px;
            width: 180px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .card h4 {
            margin: 0 0 8px;
            color: #007bff;
        }

        /* Claim button */
        .btn-claim {
            background-color: #007bff;
            color: white;
            padding: 16px 35px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            cursor: pointer;
        }

        .btn-claim:hover {
            background-color: #0056b3;
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 10px;
            font-size: 13px;
            color: #666;
            background-color: #e9ecef;
        }
    </style>
</head>

<body>

<!-- Header with Home button -->
<div class="header">
    FHPS APPLICATION
    <button class="home-btn">Home</button>
</div>

<!-- Center content -->
<div class="center-content">

    <!-- Showcase cards -->
    <div class="cards">
        <div class="card">
            <h4>Claim Status</h4>
            <p>Check your claim progress</p>
        </div>

        <div class="card">
            <h4>Eligibility</h4>
            <p>Verify patient eligibility</p>
        </div>

        <div class="card">
            <h4>Reports</h4>
            <p>View claim summaries</p>
        </div>
    </div>

    <!-- Claim button -->
    <form action="/run" method="POST">
        <input type="hidden" name="action" value="main">
        <button type="submit" class="btn-claim">
            Claim Process
        </button>
    </form>

</div>

<!-- Footer -->
<div class="footer">
    © 2025 FHPS | For internal use only
</div>

</body>
</html>
