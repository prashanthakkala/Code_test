
# Assuming the text_blocks[i] contains the information about the start and end coordinates of the lines you want to draw
if "Questions:" in text_blocks[i][4]:
    x_temp = text_blocks[i][0]  # x-coordinate where the vertical line will start (based on your text block)
    y_start = text_blocks[i][1] - 15  # y-coordinate of the starting point (slightly above the current block)
    y_end = text_blocks[i][3] + 15  # y-coordinate of the ending point (slightly below the current block)

    # Create a new shape for the vertical line
    shape = page.new_shape()

    # Draw the vertical line from (x_temp, y_start) to (x_temp, y_end)
    shape.draw_line((x_temp, y_start), (x_temp, y_end))

    # Finish and commit the line drawing with a width of 2
    shape.finish(width=2)
    shape.commit()
