<!DOCTYPE html>
<html>
<head>
    <title>FHPS Application</title>

    <style>
        body {
            margin: 0;
            height: 100vh;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            background-color: #f4f6f9;
        }

        /* Header */
        .header {
            background-color: #007bff;
            color: white;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 22px;
            font-weight: bold;
        }

        .home-btn {
            background-color: white;
            color: #007bff;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            font-size: 14px;
        }

        /* Center */
        .center-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 30px;
        }

        /* Cards */
        .cards {
            display: flex;
            gap: 20px;
        }

        .card {
            background-color: white;
            padding: 18px;
            width: 180px;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            transition: transform 0.2s;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card h4 {
            color: #007bff;
            margin-bottom: 8px;
        }

        /* Button */
        .btn-claim {
            background-color: #007bff;
            color: white;
            padding: 16px 35px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            cursor: pointer;
        }

        .btn-claim:hover {
            background-color: #0056b3;
        }

        /* Result */
        .result {
            margin-top: 20px;
            background-color: white;
            padding: 15px;
            border-radius: 6px;
            width: 60%;
            text-align: center;
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 10px;
            background-color: #e9ecef;
            font-size: 13px;
            color: #666;
        }
    </style>
</head>

<body>

<div class="header">
    FHPS APPLICATION
    <button class="home-btn">Home</button>
</div>

<div class="center-content">

    <!-- Cards -->
    <div class="cards">
        <!-- Claim Status CARD -->
        <div class="card" onclick="window.location.href='/status'">
            <h4>Claim Status</h4>
            <p>Check claim progress</p>
        </div>

        <div class="card">
            <h4>Eligibility</h4>
            <p>Verify patient eligibility</p>
        </div>

        <div class="card">
            <h4>Reports</h4>
            <p>View summaries</p>
        </div>
    </div>

    <!-- Claim Process Button -->
    <form action="/run" method="POST">
        <input type="hidden" name="action" value="main">
        <button type="submit" class="btn-claim">
            Claim Process
        </button>
    </form>

    {% if result %}
    <div class="result">
        <strong>{{ result }}</strong>
    </div>
    {% endif %}

</div>



<div class="footer">
    © 2025 FHPS | Internal Use Only
</div>

</body>
</html>


=========

@app.route('/status')
def status():
    # Run your claim status script / logic here
    status_result = run_claim_status_script()

    return render_template('template.html', result=status_result)

