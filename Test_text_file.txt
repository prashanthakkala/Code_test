# your_script.py

from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from selenium.webdriver.common.by import By

def process_function(driver):
    # Your selenium steps here
    # Example:
    driver.get("https://example.com")
    return "Process function executed."

def claim_check(driver):
    # Your selenium claim check steps here
    return "Claim check executed."

def main():
    # Initialize WebDriver ONCE here
    service = Service("path/to/chromedriver")
    driver = webdriver.Chrome(service=service)

    try:
        # Call nested functions using the SAME driver
        step1 = process_function(driver)
        step2 = claim_check(driver)

        return f"{step1} | {step2}"

    finally:
        driver.quit()


if __name__ == "__main__":
    print(main())
