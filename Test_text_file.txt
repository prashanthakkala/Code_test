import pandas as pd

# Example DataFrame
df = pd.DataFrame({
    'A': [1, 2, None],
    'B': [None, None, None],
    'C': [None, 4, None]
})

# Drop rows where all values are NaN
df_cleaned = df.dropna(how='all')

print(df_cleaned)
